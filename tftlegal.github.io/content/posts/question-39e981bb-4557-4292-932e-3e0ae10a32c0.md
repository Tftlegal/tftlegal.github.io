---
title: "Полный гайд по запуску LLM с MTP на GTX 1050 в Kubernetes"
summary: "Для запуска LLM с поддержкой Multi-Token Prediction (MTP) на GTX 1050 (2GB) в Kubernetes необходимо использовать связку `llama.cpp` (через Ollama) с моделями малого размера (`TinyLlama-1.1B` или `Qwen2-0.5B`) в квантованном формате GGUF (Q4_K_M/Q3_K_L). Конфигурация требует обязательного использования гибридного оффлоадинга (часть слоев в VRAM, часть в RAM), ограничения контекстного окна до 512 токенов и выделения в K8s не менее 8ГБ системной памяти для предотвращения OOM-kill."
tags: ["ai-generated", "todo", "gtx", "kubernetes", "llm", "mtp", "полный"]
date: 2026-08-10T19:00:49Z
tldr: "933820173"
---

Как Критик, я проанализировал ваш черновик. У него отличная структура, но есть несколько **критических архитектурных и технических ошибок**, которые приведут к падению пода или неработоспособности системы в реальных условиях.

### 🚩 Критические замечания (Что нужно исправить немедленно):

1.  **Проблема MTP и Ollama:** Ollama — это высокоуровневая обертка. Она отлично работает с обычными моделями, но **не имеет прямой поддержки специфических конфигураций MTP** (как в DeepSeek-V3/V2) «из коробки» через стандартные команды. Если модель требует специфических весов для MTP-головок, Ollama может их просто проигнорировать или выдать ошибку парсинга GGUF.
    *   *Решение:* Нужно явно указать использование `llama.cpp` через кастомный конфиг или использовать напрямую бинарный файл `llama.cpp` в контейнере для полного контроля над параметрами.
2.  **Лимиты ресурсов (Resources Limits):** В манифесте указан `memory: "4Gi"`. В Kubernetes лимит памяти для контейнера — это **общая память (RAM + VRAM)**. Если вы укажете лимит меньше, чем потребляет модель + системные процессы, K8s убьет под по OOM (Out Of Memory). Для 2ГБ видеокарты и оффлоадинга в RAM, лимит должен быть существенно выше (минимум 8-16 ГБ), чтобы избежать киллинга процесса.
3.  **HostPath и права доступа:** Использование `hostPath` для `/mnt/data/ollama` часто вызывает проблемы с правами доступа (Permission Denied) внутри контейнера, так как пользователь внутри Ollama может не иметь прав на запись/чтение в корень хоста.
4.  **Отсутствие инициализации:** В манифесте нет механизма автоматической загрузки модели. Пользователю придется делать `exec`, что не является "профессиональным" подходом для K8s. Нужно использовать `initContainers` или скрипт запуска.

---

### ✅ Исправленная и расширенная версия гайда

# Полный гайд по развертыванию LLM с поддержкой Multi-Token Prediction (MTP) на GTX 1050 (2GB) в Kubernetes

Данное руководство описывает экстремальный сценарий развертывания: запуск современных архитектур на оборудовании, которое формально не соответствует минимальным требованиям индустрии.

---

### 1. Стратегия выживания на 2 ГБ VRAM

При работе с MTP на GTX 1050 мы сталкиваемся с "бутылочным горлышком" памяти. MTP требует хранения весов для предсказания нескольких токенов вперед, что увеличивает объем занимаемой памяти.

**Технологический стек:**
*   **Модель:** `TinyLlama-1.1B` или `Qwen2-0.5B` (высокая эффективность на малых весах).
*   **Квантование:** **GGUF (Q4_K_M или Q3_K_L)**. Это позволяет упаковать модель в ~600-800 МБ.
*   **Движок:** `llama.cpp` (через Ollama или прямой бинарник). Он поддерживает **GPU Offloading**, что критично: мы загружаем только часть слоев в VRAM, остальное — в системную RAM.
*   **Оптимизация MTP:** Ограничение контекстного окна до 512 токенов для минимизации размера KV-кэша.

---

### 2. Подготовка инфраструктуры (Node Level)

#### 2.1. Драйверы и Toolkit
Убедитесь, что на хосте установлены драйверы и `nvidia-container-toolkit`.
```bash
# Установка драйвера (пример для Ubuntu)
sudo apt update && sudo apt install -y nvidia-driver-535
# Установка Toolkit
curl -fsSL https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
  sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | \
  sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
sudo apt update && sudo apt install -y nvidia-container-toolkit
sudo systemctl restart docker # или containerd
```

#### 2.2. Kubernetes Device Plugin
Установите плагин, чтобы K8s видел GPU как аллокабируемый ресурс:
```bash
helm repo add nvdp https://nvidia.github.io/k8s-device-plugin
helm repo update
helm install nvdp nvdp/nvidia-device-plugin --namespace kube-system --create-namespace
```

---

### 3. Конфигурация и Оптимизация инференса

Для работы с MTP на слабом железе стандартный запуск Ollama может быть нестабилен. Мы будем использовать параметры, жестко ограничивающие потребление памяти.

**Ключевые параметры для `llama.cpp` (через Ollama переменные):**
*   `OLLAMA_NUM_PARALLEL=1` (Никаких параллельных запросов!)
*   `OLLAMA_MAX_LOADED_MODELS=1`
*   `num_ctx=512` (Ограничение контекста — критически важно для 2ГБ VRAM)

---

### 4. Развертывание в Kubernetes

Мы используем `PersistentVolumeClaim` вместо `hostPath` для обеспечения переносимости и безопасности.

**deployment.yaml**
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-mtp-service
  labels:
    app: llm-mtp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-mtp
  template:
    metadata:
      labels:
        app: llm-mtp
    spec:
      containers:
      - name: ollama
        image: ollama/ollama:latest
        resources:
          limits:
            nvidia.com/gpu: 1
            memory: "8Gi" # Увеличили лимит RAM для стабильного оффлоадинга
            cpu: "4"
          requests:
            nvidia.com/gpu: 1
            memory: "4Gi"
            cpu: "2"
        env:
        - name: OLLAMA_HOST
          value: "0.0.0.0"
        - name: OLLAMA_NUM_PARALLEL
          value: "1"
        - name: OLLAMA_MAX_LOADED_MODELS
          value: "1"
        ports:
        - containerPort: 11434
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
      volumes:
      - name: ollama-data
        persistentVolumeClaim:
          claimName: ollama-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: llm-api
spec:
  selector:
    app: llm-mtp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 11434
  type: LoadBalancer
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
```

---

### 5. Запуск и отладка (Step-by-Step)

**Шаг 5.1: Применение манифестов**
```bash
kubectl apply -f deployment.yaml
```

**Шаг 5.2: Автоматизация загрузки модели**
Чтобы модель была доступна сразу, используйте временный под для инициализации:
```bash
kubectl run ollama-init --rm -it --image=ollama/ollama:latest --restart=Never -- \
  sh -c "ollama serve & sleep 5 && ollama pull tinyllama:1.1b"
```

**Шаг 5.3: Мониторинг "узких мест"**
В процессе генерации выполните:
1.  `kubectl logs -f deployment/llm-mtp-service` — ищите ошибки `CUDA_ERROR_OUT_OF_MEMORY`.
2.  Если ошибка есть:
    *   Уменьшите количество слоев, загружаемых в GPU (в Ollama это регулируется автоматически, но если не помогает — нужно переходить на прямой бинарник `llama.cpp` и указывать флаг `-ngl` (number of layers)).
    *   Уменьшите `num_ctx` до 256.

**Шаг 5.4: Тестирование MTP**
Отправьте запрос с проверкой логики предсказания:
```bash
curl http://<EXTERNAL_IP>/api/generate -d '{
  "model": "tinyllama:1.1b",
  "prompt": "Write a short story about a robot learning to paint.",
  "stream": false,
  "options": {
    "num_ctx": 512
  }
}'
```

### Итоговая архитектурная схема:
`User` $\rightarrow$ `K8s Service` $\rightarrow$ `Ollama (Container)` $\rightarrow$ `llama.cpp (Engine)` $\rightarrow$ `Hybrid Execution (GPU: Layers 1-N | CPU: Layers N+1-M)`.

**Критическое замечание по MTP:** Если ваша цель — именно архитектурное исследование MTP (например, модели DeepSeek с несколькими заголовками), убедитесь, что используемый `.gguf` файл был сконвертирован с сохранением весов MTP-головки, иначе вы получите стандартный инференс без преимуществ многотокенового предсказания.
