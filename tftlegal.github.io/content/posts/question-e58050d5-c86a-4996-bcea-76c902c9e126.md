---
title: "Kafka проектирование топиков, event-driven architecture, multi-DC, антипаттерны"
summary: "Проектируйте Kafka-топики вокруг событийных контрактов: домены/агрегации, уникальные имена и версии событий, partition key, порядок, retention/compaction.   Для надёжности и multi-DC используйте producers с `acks=all`, idempotence и transactions, consumers с `read_committed`, transactional offset commits, DLQ и backpressure, а также cross-DC replication/mirroring с failover и conflict resolution.   Избегайте антипаттернов — Kafka как DB/RPC, key skew, non-idempotent consumers, offset commit outside transaction, event storms, missing observability — и валидируйте сценарии нагрузкой, failover и schema compatibility."
tags: ["ai-generated", "todo", "dc", "kafka"]
date: 2026-09-17T08:01:24Z
tldr: "933820173"
---

**Работа с задачей: проектирование топиков, event-driven architecture, multi-DC и антипаттерны в Kafka**

**1. Определить границы и контракты событий**

Чтобы проектировать эффективные топики в Kafka, необходимо определить границы и контракты событий. Для этого необходимо:

*   Разбить домен на отдельные домены, связывающиеся с конкретными бизнес-процессами.
*   Определить aggregration для каждого домена, т.е. группу событий, которые можно обрабатывать вместе в единицу времени.
*   Выбрать уникальные имена событий, которые будут использоваться для обмена между брокерами и приложениями.
*   Установить версии для событий, чтобы можно было различать различные версии события при обмене между брокерами и приложениями.
*   Выбрать ключи для идентификации событий, чтобы можно гарантировать, что каждый событий будет обработан только один раз.
*   Использовать correlation_id для идентификации событий, которые связаны с одним и тем же бизнес-процессом.
*   Установить требования к порядку для каждого события, чтобы можно garantировать, что события обрабатываются в правильном порядке.

**2. Спроектировать топики под EOS и multi-DC**

Чтобы проектировать эффективные топики в multi-DC, необходимо:

*   Следить за тем, чтобы имена событий были уникальными и соответствовали доменам и aggregration.
*   Выбрать ключи для partitioning, чтобы можно было efficiently обрабатывать события.
*   Выбрать количество партиций, чтобы можно было efficiently обрабатывать события.
*   Установить retention/compaction для каждого топика, чтобы можно гарантировать, что события не утащились.
*   Установить replication.factor, чтобы можно garantировать, что события replicationed на каждом брокере.
*   Установить min.insync.replicas, чтобы можно garantировать, что события replicationed не менее min.insync.replicas на каждом брокере.
*   Выбрать стратегию репликации, чтобы можно garantировать, что события replicationed между DC.

**3. Настроить producers на надёжную запись**

Чтобы проектировать надёжные producers в Kafka, необходимо:

*   Включить acks=all, чтобы можно garantировать, что события записаны на всех брокерах.
*   Включить enable.idempotence=true, чтобы можно garantировать, что события обрабатываются только один раз.
*   Установить transactional.id, чтобы можно garantировать, что события записаны в одной транзакции.
*   Установить batching/retries для каждого producers, чтобы можно garantировать, что события обрабатываются efficiently.
*   Обработывать ошибки для каждого producers, чтобы можно garantировать, что события обрабатываются только один раз.
*   Использовать Outbox Pattern для обмена с внешними системами, чтобы можно garantировать, что события записываются в одинаковой форме.

**4. Спроектировать consumers с read_committed и идемпотентностью**

Чтобы проектировать эффективных consumers в Kafka, необходимо:

*   Установить consumer groups per DC, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить isolation.level=read_committed, чтобы можно garantировать, что события обрабатываются только один раз.
*   Использовать Read-Process-Write, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить offset commit inside Kafka transaction, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить retries для каждого consumers, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить DLQ, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить backpressure, чтобы можно garantировать, что события обрабатываются efficiently.

**5. Проектировать multi-DC flow**

Чтобы проектировать multi-DC flow в Kafka, необходимо:

*   Выбрать стратегию active-active или passive, чтобы можно garantировать, что события обрабатываются efficiently.
*   Выбрать partition ownership, чтобы можно garantировать, что события обрабатываются efficiently.
*   Выбрать cross-DC mirroring/replication, чтобы можно garantировать, что события replicationed между DC.
*   Включить failover, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить conflict resolution, чтобы можно garantировать, что события обрабатываются efficiently.
*   Выбрать стратегию latency/consistency tradeoffs, чтобы можно garantировать, что события обрабатываются efficiently.

**6. Обеспечить консистентность с внешними системами**

Чтобы обеспечить консистентность с внешними системами в Kafka, необходимо:

*   Использовать Outbox Pattern для обмена с внешними системами, чтобы можно garantировать, что события записываются в одинаковой форме.
*   Использовать idempotent side effects, чтобы можно garantировать, что события обрабатываются efficiently.
*   Использовать sagas/orchestrators, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить retries, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить compensation, чтобы можно garantировать, что события обрабатываются efficiently.
*   Включить external event acknowledgment, чтобы можно garantировать, что события обрабатываются efficiently.

**7. Выстроить антипаттерн-чеклист**

Чтобы выстроить антипаттерн-чеклист в Kafka, необходимо:

*   Убедитесь, что Kafka не используется как DB/RPC, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что количество тем и партиций неtoo high, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что key skew не_exists, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что ordering violations не_occurren, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что consumers non-idempotent не_uses, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что offset commit не_occurree outside transaction, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что Cross-DC exactly-once myths не_uses, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что event storms не_occurren, чтобы можно garantировать, что события обрабатываются efficiently.
*   Убедитесь, что DLQ/monitoring нет, чтобы можно garantировать, что события обрабатываются efficiently.

**8. Запланировать валидацию и операции**

Чтобы запланировать валидацию и операции в Kafka, необходимо:

*   Запланивает load/failover tests, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиuates ISR shrink, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиulates cross-DC failure, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиulates transaction fencing, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиulates schema compatibility, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиulates observability, чтобы можно garantировать, что события обрабатываются efficiently.
*   Запланиulates runbooks, чтобы можно garantировать, что события обрабатываются efficiently.

**Баги и ошибки**

*   **Key skew**: ключевая проблема в том, что key skew может привести к inefficientному обслуживанию событий.
*   **Non-idempotent consumers**: неидемпотентные consumers могут привести к inefficientному обслуживанию событий.
*   **Offset commit outside transaction**:offset commit outside transaction может привести к inefficientному обслуживанию событий.
*   **Cross-DC exactly-once myths**: Cross-DC exact-once myths могут привести к inefficientному обслуживанию событий.
*   **Event storms**: event storms могут привести к inefficientному обслуживанию событий.
*   **DLQ/monitoring**: DLQ/monitoring отсутствия могут привести к inefficiente обслуживанию событий.

**Советы и рекомендации**

*   **Применяйте принципы изделия**: примите принципы изделия для проектирования эффективных топиков в Kafka.
*   **Используйте Kafka Streams**: utilize Kafka Streams для обрабатывания событий и обеспечения консистентности.
*   **Используйте Outbox Pattern**: utilize Outbox Pattern для обмена с внешними системами.
*   **Используйте idempotent side effects**: utilise idempotent side effects для обрабатывания событий и обеспечения консистентности.
*   **Используйте sagas/orchestrators**: utilise sagas/orchestrators для обрабатывания событий и обеспечения консистентности.
*   **Используйте retries**: utilise retries для обрабатывания событий и обеспечения консистентности.
*   **Используйте compensation**: utilise compensation для обрабатывания событий и обеспечения консистентности.
*   **Используйте external event acknowledgment**: utilise external event acknowledgment для обрабатывания событий и обеспечения консистентности.
